先到 GO 官方網
用 wget 把 ARMv6的類型載下來
在這同時 去GETH的官方網

載完GO 把載下來的tar黨解壓縮
就會出現 go的資料夾 然後就可以把 tar黨砍掉了 

把go/bin/go 複製貼上到 /usr/local/bin  記得用sudo

如果輸入 go指令有東西 就代表安裝成功

再來就是安裝Geth

一樣的方式去官網 -> 載點 -> wget -> 解壓縮 

記得找Geth&Tools的

載完進到 資料夾裡面
把資料夾裡面的東西 除了COPYING以外 全部複製貼上到 /usr/local/bin

輸入指令後有東西 表示安裝成功!!!